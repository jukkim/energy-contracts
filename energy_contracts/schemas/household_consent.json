{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "energy-contracts/household_consent.json",
  "title": "HouseholdConsent",
  "description": "가정 1세대의 **직접부하제어 참여 동의**. 화면(AIRO Home)이 받은 선택과 엣지(edge-agent)가 강제하는 범위가 같은 정의를 읽게 하는 계약. ⚠ 제어 명령 계약(control_command.json)과 **별개 축**이다 — 명령에 동의 범위(델타·횟수)를 실으면 명령마다 동의가 재협상되는 꼴이 되고, 동의는 명령보다 수명이 길다.",
  "_usage": "hybrid",
  "_consumers": [
    "edge-agent",
    "airos-energy-decision"
  ],
  "version": "1.1",
  "type": "object",
  "$defs": {
    "ConsentPresetId": {
      "type": "string",
      "enum": [
        "light",
        "standard",
        "deep"
      ],
      "description": "동의 프리셋 식별자. **정본 3 종**이며 기본 선택은 없다(가구가 고르지 않으면 동의가 아니다). 값·라벨은 아래 `default.presets` 가 정본."
    },
    "ParticipationState": {
      "type": "string",
      "enum": [
        "NOT_ENROLLED",
        "OPTED_IN",
        "OPTED_OUT",
        "OFFLINE"
      ],
      "description": "참여 상태. ⚠ 넷을 뭉치면 거짓이 된다 — 두절(OFFLINE)을 철회(OPTED_OUT)로 세면 동의한 가구를 잃고, 철회를 두절로 세면 **거부한 집에 계속 명령을 보낸다**. NOT_ENROLLED = 한 번도 동의한 적 없음(두절과 다르다)."
    },
    "ConsentEnvelope": {
      "type": "object",
      "description": "가구가 동의한 **범위**. 세 축을 모두 갖는다 — *\"에어컨 1℃ 까지\"* 와 *\"3℃ 까지\"* 는 다른 동의이고, 범위 없는 동의는 동의가 아니다.",
      "required": [
        "max_setpoint_delta_c",
        "max_duration_min",
        "max_events_per_day"
      ],
      "properties": {
        "max_setpoint_delta_c": {
          "type": "number",
          "minimum": 0,
          "maximum": 5,
          "description": "설정온도를 기준선 대비 몇 ℃ 까지 **올려도** 되는가(냉방 기준). ⚠ 기준선은 '현재값' 이 아니라 `현재 − 지금 걸린 제어` 다 — 현재값으로 재면 24→26→28 로 동의의 두 배를 쓴다. ⚠ **내리는 축은 이 계약에 없다**(예냉 등은 별도 동의 사안)."
        },
        "max_duration_min": {
          "type": "integer",
          "minimum": 0,
          "maximum": 480,
          "description": "한 번에 몇 분까지. 명령이 지속시간을 안 밝히면 이 값을 상한으로 적용한다 — 모른다고 무제한으로 두면 동의한 시간을 넘겨 걸린다."
        },
        "max_events_per_day": {
          "type": "integer",
          "minimum": 0,
          "maximum": 24,
          "description": "**하루** 몇 회까지. 동의는 횟수도 갖는다. ⚠ 날짜 경계에서 리셋되지 않으면 이 값은 하루 한도가 아니라 **평생 한도**가 된다(상시 가동 엣지에서 N 회 이후 전건 거부)."
        }
      }
    },
    "ConsentPreset": {
      "type": "object",
      "description": "화면에 내놓는 선택지 하나. **숫자를 그대로 표시**해야 한다 — 라벨만 보여주면 무엇에 동의했는지 가구가 알 수 없고, 그건 동의가 아니라 위임이다.",
      "required": [
        "preset_id",
        "label_ko",
        "envelope"
      ],
      "properties": {
        "preset_id": {
          "$ref": "#/$defs/ConsentPresetId"
        },
        "label_ko": {
          "type": "string",
          "description": "화면 표시 라벨(한국어)."
        },
        "envelope": {
          "$ref": "#/$defs/ConsentEnvelope"
        }
      }
    },
    "ConsentRequest": {
      "type": "object",
      "description": "참여 동의 등록·변경 요청 (PUT). 가구가 고른 프리셋을 보낸다.",
      "required": [
        "preset_id"
      ],
      "properties": {
        "preset_id": {
          "$ref": "#/$defs/ConsentPresetId"
        },
        "contract_version": {
          "type": "string",
          "description": "요청자가 읽은 이 계약의 버전. 미기재 허용(수신자 버전으로 간주)."
        },
        "displayed": {
          "$ref": "#/$defs/ConsentEnvelope",
          "description": "**가구에게 실제로 보여준 숫자.** 선택. 보내면 수신자가 정본 프리셋과 대조해 **다르면 거부**한다 — 화면이 '2℃' 라 말하고 엣지가 3℃ 를 거는 드리프트를 계약 단계에서 잡는다."
        }
      }
    },
    "ConsentState": {
      "type": "object",
      "description": "현재 동의 상태 (GET 응답). 화면이 '남은 횟수' 를 스스로 계산하지 않아도 되게 파생값을 함께 준다.",
      "required": [
        "household_id",
        "participation",
        "day",
        "events_today",
        "remaining_events_today",
        "contract_version"
      ],
      "properties": {
        "household_id": {
          "type": "string"
        },
        "participation": {
          "$ref": "#/$defs/ParticipationState"
        },
        "preset_id": {
          "oneOf": [
            {
              "$ref": "#/$defs/ConsentPresetId"
            },
            {
              "type": "null"
            }
          ],
          "description": "동의에 쓰인 프리셋. 미동의(NOT_ENROLLED) 시 null."
        },
        "contract_version": {
          "type": "string",
          "description": "이 상태를 강제하는 계약 버전."
        },
        "consent": {
          "oneOf": [
            {
              "$ref": "#/$defs/ConsentEnvelope"
            },
            {
              "type": "null"
            }
          ],
          "description": "적용 중인 범위. 미동의 시 null."
        },
        "applied_delta_c": {
          "type": "number",
          "description": "지금 걸려 있는 제어(℃). 기준선 계산에 쓰인다."
        },
        "events_today": {
          "type": "integer",
          "minimum": 0,
          "description": "오늘 쓴 횟수."
        },
        "remaining_events_today": {
          "type": "integer",
          "minimum": 0,
          "description": "**오늘 남은 횟수** = max(0, max_events_per_day − events_today). ⚠ 소비자가 직접 빼서 쓰면 날짜 롤오버 시점이 어긋난다(엣지는 넘어갔는데 화면은 어제 값으로 뺀다) — 그래서 파생을 **한 곳에서** 한다. 미동의 시 0."
        },
        "day": {
          "type": "string",
          "format": "date",
          "description": "`events_today` 가 속한 날짜(엣지 로컬 TZ). 조회 시점에 롤오버가 반영된 값이어야 한다."
        },
        "capacity": {
          "type": "object",
          "additionalProperties": true,
          "description": "상위 재계획 입력(동원 가능 자원). 철회=영구 감소 · 두절=일시 감소로 갈라 센다."
        },
        "requires_reconsent": {
          "type": "boolean",
          "default": false,
          "description": "정본 프리셋과 맞지 않는 **구 동의**를 들고 있었다는 표시. 이 상태에서는 참여로 세지 않는다(명령 전건 거부) — 화면은 가구에게 **다시 물어야** 한다. ⚠ 숫자가 정본에 없다는 것은 가구가 지금 표에 없는 범위에 동의했다는 뜻이고, 그 동의를 임의의 프리셋으로 승격하면 동의하지 않은 범위를 적용하게 된다."
        }
      }
    }
  },
  "properties": {
    "presets": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "items": {
        "$ref": "#/$defs/ConsentPreset"
      },
      "description": "정본 프리셋 3 종. 화면·엣지가 **같은 표**를 읽는다."
    }
  },
  "default": {
    "presets": [
      {
        "preset_id": "light",
        "label_ko": "조금만",
        "envelope": {
          "max_setpoint_delta_c": 1.0,
          "max_duration_min": 120,
          "max_events_per_day": 1
        }
      },
      {
        "preset_id": "standard",
        "label_ko": "보통",
        "envelope": {
          "max_setpoint_delta_c": 2.0,
          "max_duration_min": 120,
          "max_events_per_day": 2
        }
      },
      {
        "preset_id": "deep",
        "label_ko": "많이",
        "envelope": {
          "max_setpoint_delta_c": 3.0,
          "max_duration_min": 240,
          "max_events_per_day": 3
        }
      }
    ]
  }
}
