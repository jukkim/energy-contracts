"""Computer Profile 헬퍼 (Phase B-2 / J-6).

SSOT: schemas/computer_profile.json (machines.A.detection.path 등)
6 프로젝트의 _generated_constants 에 COMPUTER_PROFILES 로 export 됨.

사용 예시 (각 프로젝트 코드):
    from <pkg>._generated_constants import COMPUTER_PROFILES
    if detect_machine() == "A":
        ...
"""
# 본 파일은 reference. 각 프로젝트는 자체 _generated_constants 의 COMPUTER_PROFILES 사용.
