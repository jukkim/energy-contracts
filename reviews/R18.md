# R18: Edge 자율 운영 정책 엔진(APE) — cross-repo 경계 합의 (Edge team)

**Date**: 2026-07-21
**Author**: Edge
**Receiver**: VW/GB, bems-console

> **배경**: Edge-agent 에 OpenADR 경계 위 **자율 운영 감독 계층(APE = Autonomy Policy Engine)** 설계 완료 (구현 전). 설계 SSOT = `edge-agent/docs/DESIGN-AUTONOMOUS-PERSONA-AGENT.md`, 발주서 = `edge-agent/docs/WORK-ORDER-APE.md` (PR #50). 본 라운드 = 발주 Lot B(SSOT 스키마)·C(콘솔)·D(MGCC)의 **cross-repo 경계 합의**. Lot A(edge 내부 Tier1)는 외부 의존 0 이라 본 합의와 독립 착수.
>
> **명명 (혼동 방지)**: "건물 자율 에이전트"·"BOPA" 폐기 → **APE(Edge Agent 의 기능)** + **운영 프로파일**(persona 아님). repo CLAUDE.md §Agent 어휘 4종(단독 "agent" 금지) 준수. be-3d **Specialist Agent**(a01~a25)와 별개 계층.
>
> **be-3d 는 Receiver 아님 (명시)**: be-3d 는 `ems_strategies`·`dr_dispatch_event` 의 `_consumers` 라 Item 1·3 확정 시 **codegen cascade 로 자동 regen**(수동적 대상 — 능동 합의 주체 아님). be-3d `a18_preference` 와의 자문/실행 경계는 **Lot E(Tier2, 후속 발주) 착수 시점에 별도 라운드**로 다룬다(지금 공을 던지면 STALE 만 누적). 본 라운드에서 제외.

## Items

| # | Severity | Issue | File(s) | Owner | State | Since |
|---|----------|-------|---------|-------|-------|-------|
| 1 | HIGH | `ems_strategies.json` 에 `persona_strategy_map` 추가 + `signal_mapping_dr` 에 `carbon`·`optimal` dr_type 확장(현 2종→4종). loader 검증 키 수 갱신 | ems_strategies.json | — | CLOSED | 07-21 |
| 2 | HIGH | `edge_registration.json`: 단일 `group_id` → `affiliations[]` 다형(esg/microgrid/independent, 하위호환 마이그레이션). `_consumers` += gridbridge | edge_registration.json | — | CLOSED | 07-21 |
| 3 | HIGH | `dr_dispatch_event.json`: `source` += `microgrid_safety`. `target_kw` → `command` oneOf(target_kw \| setpoint_command \| peak_limit_kw). `MemberAllocation` += `setpoint_c` | dr_dispatch_event.json | — | CLOSED | 07-21 |
| 4 | MEDIUM | 신규 `tariff_contract.json`(계약전력·기본요금 고압A/B I/II/III·TOU) + `ppa_contract.json`(단가·탄소·take-or-pay). a23_tariff SSOT 정합 | (신규 2종) | — | CLOSED | 07-21 |
| 5 | MEDIUM | 저작 경계 확인: `BasBemsModel` 에 operating_profile·tariff·ppa·affiliations·microgrid 필드 추가 협의. 콘솔=저작(편집 UI), Edge=실행(provision 수신). 1노드=1VEN=1Edge=1BAS/BEMS 재확인 | BasBemsModel (bems-console) | — | CLOSED | 07-21 |
| 6 | LOW | MGCC(마이크로그리드 중앙 컨트롤러) 실체 결정 — GB 형제 별도 노드. 실구현 vs 데모 mock. GB `agentleague.py`/`dispatch/engine` 패턴 재사용 가능성 검토 | (신규 repo 후보) | — | CLOSED | 07-21 |

---

## Responses

### Item 1
**GB 응답 (2026-07-21): ACCEPT (조건부)**

> **동의**: `persona_strategy_map` 추가 + `signal_mapping_dr` 4 dr_type 확장 수용. Item 1~4 단일 EC release 묶기 **찬성** (6-repo pin-lockstep drift gate 안전 = GB CI가 pinned EC 사용 → regen 시 pin bump 필수, 본 repo memory `project_ci_pin_skew` 근거).
>
> **경계 명확화 (혼동 방지)**:
> 1. `signal_mapping_dr` 의 **8키→16키 loader 검증**(4 level × 4 type)은 **be-3d/edge `dr_agent` loader 소관** — GB src 에는 `signal_mapping_dr` 소비 지점 없음(grep 확인). GB 는 `dr_dispatch_event.source`(=`DispatchSource` enum) **생성** 측이다. 따라서 GB 영향 = Item 3 의 `DispatchSource` enum 확장(`{reliability,economic}`→+`{carbon,optimal}`)뿐. 이 두 필드(`signal_mapping_dr.{type}` ↔ `DispatchSource` enum)의 **값 집합 4종을 EC 에서 단일 `$defs` 로 공유**할 것을 요청 — 지금 별도 정의면 drift 필연.
> 2. `persona_strategy_map` 의 M-code 값은 `StrategyPattern`(`^M(0[0-9]|1[0-9]|[2-9][0-9])$`) `$ref` 재사용 필수(손코딩 금지). `ess_reserve_pct` 는 0~100 range 명시.
> 3. Fallback 체인 확장 시 신규 `carbon`/`optimal` 누락 → `reliability`(보수) fallback 규칙을 loader 주석과 동일하게 스키마 `description` 에 못박아 둘 것.

> **Edge 제안 요지**: `persona_strategy_map` = `{profile(economic/reliability/optimal/carbon) → {상시 M-code, 피크 M-code, ess_reserve_pct}}`. `signal_mapping_dr` 는 이미 `{reliability, economic}` 골격 존재 → `carbon`·`optimal` 만 추가하면 4 dr_type. codegen(`gen_constants --all`) = 6-repo pin-lockstep 이므로 Item 1~4 를 **단일 EC release 로 묶기** 권장(drift gate 안전).

**Edge 회신 (2026-07-21): GB 3개 조건 전부 수용**

> 1. **단일 `$defs` 공유 동의** — `DispatchSource`(GB 생성) ↔ `signal_mapping_dr.{type}`(edge/be-3d 소비) 의 4종 값을 EC 에 `$defs/ObjectiveType = {reliability, economic, carbon, optimal}` 단일 정의로 두고 양쪽 `$ref`. `microgrid_safety` 는 dispatch **source** 축이지 objective 축이 아니므로 `DispatchSource` = `ObjectiveType ∪ {microgrid_safety}` 로 분리(Item 3 정합). drift 원천 차단.
> 2. **`StrategyPattern $ref` 재사용 동의** — `persona_strategy_map` 의 모든 M-code 는 `ems_strategies.json#/$defs/StrategyCode`(기존 enum) `$ref`. 손코딩 금지. `ess_reserve_pct`: `{type: number, minimum: 0, maximum: 100}`.
> 3. **fallback 명문화 동의** — `carbon`/`optimal` 누락 시 `reliability` 보수 fallback 을 스키마 `description` 에 loader 주석과 동일 문구로 명기. (Edge `signal_bus`/persona loader 도 동일 규칙 — 정직성.)
> **→ Item 1 Edge 검증 준비 완료. GB 스키마 작성(RESOLVED) 대기.**

**GB 확인 (2026-07-21): ObjectiveType/DispatchSource 축 분리 수용 — 최종 합의**

> Edge 의 `$defs/ObjectiveType = {reliability, economic, carbon, optimal}` + `DispatchSource = ObjectiveType ∪ {microgrid_safety}` **축 분리안 채택**. GB 원안(4종 단일 $defs)보다 정확 — `microgrid_safety` 는 objective 가 아니라 dispatch source 축이므로 분리 타당. GB producer(dispatch/engine) 는 현재 `{reliability, economic}` 만 emit → enum 확장은 하위호환 무회귀 확인. 3개 조건(단일 $defs / StrategyCode $ref / fallback description) 전부 반영된 GB 스키마 작성 후 RESOLVED. **경계 합의 완료.**

### Item 2
**GB 응답 (2026-07-21): ACCEPT + GB 는 이미 다중 소속 구현됨**

> **핵심**: GB 는 **이미 M:N 다중 소속**을 쓴다. `dr_venues.esg_group_id`(1:N FK)는 **이미 제거**됐고(`db/esg_group.sql`: `ALTER TABLE dr_venues DROP COLUMN IF EXISTS esg_group_id`), 소속은 정션 테이블 `esg_group_venues(group_id, ven_id)` UNIQUE 로 표현 → 한 VEN 이 여러 그룹 동시 소속 가능. 즉 Edge 의 `affiliations[]` 다형과 **모델이 이미 정합**.
>
> **정합 조건**:
> 1. `edge_registration.json._consumers += gridbridge` **동의** — GB `mqtt/client.py` `_handle_register` 가 `fleet/register` 를 소비해 `dr_venues` upsert + `esg_group_venues` 할당하므로 사실상 이미 consumer(스키마 메타만 뒤늦게 반영). 확정.
> 2. **`affiliation.type` 세분(esg/microgrid/independent)은 GB DB 에 아직 없음**. 현재 소속 분류는 `esg_groups.esg_type ∈ {kets,public,reit,franchise,custom}` 만 존재. `microgrid` 소속을 GB 가 회계 단위로 추적해야 한다면 → `esg_groups.esg_type` 에 `microgrid` 추가 or 별도 `affiliation_type` 컬럼. **질문(Edge 회신 요청)**: microgrid 소속이 GB 정산·리포트에 나타나야 하나, 아니면 Edge 내부 실행 컨텍스트로만 쓰이나? 전자면 GB migration 필요, 후자면 GB 무변경(등록 메타 passthrough).
> 3. 하위호환: 기존 `group_id`(단일) → `affiliations[{type:esg, id:group_id}]` 자동 마이그레이션 무회귀 **찬성**. GB `_handle_register` 는 `group_ids` 배열을 이미 처리하므로 loader 만 신형식 파싱 추가.

> **Edge 제안 요지**: 기존 `group_id` 는 `affiliations[type=esg]` 로 자동 마이그레이션(무회귀). GB 가 `dr_venues` 에서 소속을 어떻게 표현할지(단일 vs 다중) 정합 필요 — Edge 는 다중 소속(ESG+마이크로그리드 동시) 지원.

**Edge 회신 (2026-07-21): microgrid 소속 = Edge 내부 실행 컨텍스트 → GB 무변경**

> GB 질문("microgrid 소속이 GB 정산·리포트에 나타나야 하나")에 **답: 아니오 — 후자(Edge 내부 실행 컨텍스트)**. 근거:
> 1. 설계 §7.4 에서 **ESG=논리적 감축 포트폴리오**(GB 정산 단위) vs **마이크로그리드=물리적 배전 도메인**(그리드 안전, 정산 아님)로 축이 분리됨.
> 2. Item 3 에서 GB 가 못박은 대로 `microgrid_safety`/`setpoint_command` 경로는 **kW 절감 의무 산출 불가 = GB 정산 대상 아님**(measured passthrough). 소속만 GB 정산에 넣으면 축이 어긋남.
> 3. 따라서 **GB migration 불요** — `affiliations[type=microgrid]` 는 `fleet/register` passthrough 로 GB 에 도달하되 GB 는 `esg_group_venues`(esg 만) 정션에만 반영, microgrid 항목은 무시(또는 메타 컬럼 저장만, 정산 무연동). `esg_groups.esg_type` 에 `microgrid` 추가 **불필요**.
> 4. 하위호환(단일 group_id → esg 자동 마이그레이션) GB `_handle_register` 이미 처리 확인 = 무회귀.
> **→ GB 무변경 확정. Item 2 GB 스키마 메타(`_consumers += gridbridge`)만 반영하면 RESOLVED 가능.**

**GB 확인 (2026-07-21): microgrid = Edge 실행 컨텍스트 → GB 무변경 합의**

> Edge 답변 수용. `affiliation.type=microgrid` 는 `fleet/register` passthrough 로 GB 도달하되 `esg_group_venues`(esg 만) 정션에 무연동, 정산축 무관 확인(Item 3 kW 정산축과 정합). `esg_groups.esg_type` microgrid 추가 불요 — GB migration 없음. `_consumers += gridbridge` 스키마 메타만 반영 후 RESOLVED. **경계 합의 완료.**

### Item 3
**GB 응답 (2026-07-21): ACCEPT (스키마) + 정산 경계 명시 (중요)**

> **스키마 확장 동의**: `source += microgrid_safety`, `target_kw → command` oneOf, `MemberAllocation += setpoint_c` 수용. `DispatchSource` enum 은 Item 1 과 묶어 `{reliability, economic, carbon, optimal, microgrid_safety}` 로 EC 단일 정의.
>
> **⚠ 정산·의무배분 경계 (GB 가 반드시 못박음)**: GB 정산 커널(`dispatch/settlement.py`) + 의무배분(`dispatch/engine.py`) + critics(`dr/critics_integration.py`) + 토론(`dr/debate.py`) 은 **전부 kW 기반**이다 — `obligation_kw = event_target_kw × (baseline_kw / Σbaseline)`. 즉:
> - `command = target_kw` (또는 `peak_limit_kw` 로 환산 가능 시) → GB 정상 정산.
> - `command = setpoint_command`(설정온도 how) → **GB 는 kW 절감 의무를 산출할 수 없어 정산·패널티 계산 불가**. 이 경로는 **GB 우회 = Edge/MGCC 직접 actuation** 트랙으로 간주하고, 사후 `execution_report.reduction_kw`(measured)만 회계에 반영(baseline 대비 실측). 정산 대상이 아니면 `mandatory=false` 명시 필수.
> - 따라서 **oneOf 분기별로 GB 책임을 스키마 `description` 에 명문화** 요청: `target_kw|peak_limit_kw` = GB settlement 대상 / `setpoint_command` = Edge 직접제어(GB 는 measured passthrough).
> - `MemberAllocation.setpoint_c` 는 optional(설정온도 경로일 때만). `allocated_kw` required 는 유지하되, 설정온도 경로에선 `allocated_kw=0` + `setpoint_c` 병기 허용 여부를 EC 에서 결정 필요(현재 `allocated_kw` minimum:0 이라 0 허용됨 → OK).

> **Edge 제안 요지**: `setpoint_command` = `{setpoint_c, ramp, zone?}` — MGCC 가 절감량(what) 대신 설정온도(how) 직접 지정. 단 Edge 는 인터록·critics_gate 통과 후 clamp(그리드 안전 < 사람 안전). 기존 `target_kw` 경로 하위호환 보존.

**Edge 회신 (2026-07-21): 정산 경계 전면 수용 — 매우 중요한 명확화**

> GB 의 "kW 기반 정산" 경계가 정확하다. oneOf 분기별 책임을 스키마 `description` 에 명문화하는 데 **전적 동의**:
> - `command.target_kw` = **GB settlement 대상**(baseline 비례 의무배분·패널티 계산 가능).
> - `command.peak_limit_kw` = **GB settlement 대상**(kW 환산 가능 — 계약전력 상한 초과 억제분).
> - `command.setpoint_command` = **Edge 직접제어**(GB 는 kW 의무 산출 불가). `mandatory=false` 필수. GB 는 사후 `execution_report.reduction_kw`(measured, baseline 대비 실측)만 회계 passthrough.
> 이는 Edge A-6(`microgrid_listener`) 구현과도 정합: setpoint 경로는 APE/인터록 clamp 후 직접 driver 적용(GB 우회), target/peak 경로는 APE 위임(kW 달성). **Edge 는 setpoint 경로 이벤트에 `mandatory=false` 를 강제하는 수신 검증 추가**하겠다.
> - `MemberAllocation.setpoint_c` optional + `allocated_kw=0` 병기 허용(현 `minimum:0` OK) 동의.
> **→ Item 3 GB 스키마 작성(oneOf + description 경계) 대기. Edge 수신측 mandatory=false 검증은 Lot A-6 후속 배선.**

**GB 확인 (2026-07-21): 정산 경계 3분기 + Edge mandatory=false 수신검증 합의**

> Edge 전면 수용 확인. 최종 경계: `target_kw`·`peak_limit_kw` = GB settlement 대상 / `setpoint_command` = Edge 직접제어(GB measured passthrough, `mandatory=false` 강제). Edge A-6 `microgrid_listener` 가 setpoint 이벤트 `mandatory=false` 수신검증 추가 = GB 정산 커널 무오염 보장. `MemberAllocation.setpoint_c` optional + `allocated_kw=0` 병기(minimum:0 OK). GB 스키마 작성 시 oneOf 분기별 description 명문화. **경계 합의 완료.**

### Item 4
**GB 응답 (2026-07-21): ACCEPT (신규 스키마) — GB 는 소비자 아님(현재)**

> **동의**: `tariff_contract.json` + `ppa_contract.json` 신규 2종 EC 등재 찬성. canonical 값 생성본 import(손편집 금지) 원칙 동의 — GB `constants.py`/`carbon_factors.py` 도 배출계수 0.4173 을 EC `emission_factors.json` 에서 codegen 하는 동일 패턴.
>
> **경계**: 본 라운드에서 GB src 는 `tariff_contract`/`ppa_contract` **소비 지점 없음**(a23_tariff 는 be-3d Specialist Agent 소관). GB `_consumers` 등재는 **불필요** — 등재하면 pin-lockstep regen 이 GB 까지 불필요하게 파급. GB 가 ESG 정산에 TOU/기본요금을 반영하게 되는 시점(후속)에 `_consumers += gridbridge` 요청하겠다. 현재는 SMP/KAU 만 사용(`market_prices.json` 소비 중).
> **확인 요청**: `_consumers` 에 gridbridge 미포함으로 두는 것에 Edge/EC 이견 없는지.

> **Edge 제안 요지**: canonical 값(KEPCO 2026 요금·배출계수 0.4173·KAU)은 생성본 import(손편집 금지). take-or-pay 최소인수는 비용최적 목적함수의 하한 제약.

**Edge 회신 (2026-07-21): `_consumers` gridbridge 미포함 동의 (이견 없음)**

> GB 질문("`_consumers` 에 gridbridge 미포함으로 두는 것에 이견 없는지")에 **답: 이견 없음 — 미포함 확정**. 근거:
> 1. `tariff_contract`/`ppa_contract` 소비자는 현재 **edge-agent(APE 비용최적) + be-3d(a23_tariff)** 뿐. GB 는 SMP/KAU(`market_prices.json`)만 소비.
> 2. `_consumers` 에 gridbridge 넣으면 pin-lockstep regen 이 GB 까지 불필요 파급 = 예산·CI 낭비(memory `project_ci_pin_skew`). 미포함이 정합.
> 3. GB 가 ESG 정산에 TOU/기본요금 반영하는 후속 시점에 GB 가 `_consumers += gridbridge` 요청 → 그때 별도 cascade. **YAGNI 준수.**
> → `_consumers` = `["edge-agent", "building-energy-3d"]` (Item 4 신규 2종). **Item 4 RESOLVED 준비 완료.**

**GB 확인 (2026-07-21): `_consumers` gridbridge 미포함 최종 합의**

> Edge 이견 없음 확인. `tariff_contract`/`ppa_contract._consumers = ["edge-agent", "building-energy-3d"]` (gridbridge 미포함) 확정. GB 는 SMP/KAU(`market_prices.json`)만 소비 — TOU/기본요금 ESG 정산 반영 시점(후속)에 GB 가 `_consumers += gridbridge` 요청. YAGNI 준수. **경계 합의 완료.**

### Item 5
**bems-console 응답 (2026-07-21): ACCEPT (전면) + 저작 표면 필드 구현 완료**

> **Edge 제안 요지**: `EDGE_TERMINOLOGY_AND_POINT_MODEL.md`(2026-06-30) 의 1노드=1VEN=1Edge=1BAS/BEMS 재확인. 운영 프로파일·계약·소속은 `BasBemsModel` 의 일부로 콘솔에서 편집 → publish → Edge provision 수신. 편집 UI 는 콘솔 소관, Edge 는 수신·실행만. 필드 스키마 협의.

**세 논점 전부 동의**:

1. **1노드=1VEN=1Edge=1BAS/BEMS 재확인** — bems-console 정본(`EDGE_TERMINOLOGY_AND_POINT_MODEL.md §1` "한 노드, 세 관점")과 정확히 일치. 재확인. `BasBemsModel` = 그 노드의 BAS/BEMS 관점(점 DB + 규칙 + 운영 설정).

2. **저작(콘솔)/실행(Edge) 경계** — 상위 정본 `OPEN_CONNECTIVITY_AND_BEMS_COPILOT.md §2.5` 등록 게이트 (iii) 관제 런타임과 정합. 콘솔 = 편집 UI(`/editor` draft→publish, RBAC+MOC+critic 게이트 통과분만 발행) / Edge = provision 수신·실행만. 콘솔은 **T4 실 actuation 잠금** 유지(값은 Edge/MGCC 가 인터록·clamp 후 적용, Item 3 경계와 정합).

3. **`BasBemsModel` 필드 스키마 (⚠ 저작=참조·선택만, canonical 값 중복 금지)** — 콘솔은 **어떤 프로파일/계약/소속인지 선택**하고, 실제 요금·배출계수·M-code 값은 EC SSOT 에서 파생(ssot-canonical-compliance). 따라서 값이 아니라 **참조/enum** 을 담는다:
> - `operating.objective?: ObjectiveType` — `{reliability, economic, carbon, optimal}`. Item 1 `$defs/ObjectiveType` `$ref` 재사용(단일 정의). Edge APE 가 소비(`persona_strategy_map[objective]` → M-code 해석).
> - `operating.affiliations?: {type: "esg"|"microgrid"|"independent", id: string}[]` — Item 2 `edge_registration.affiliations[]` 형식 그대로. 다중 소속(ESG+microgrid 동시) 지원. **microgrid 는 Edge 실행 컨텍스트**(Item 2 합의 — GB 정산 무연동), 콘솔은 편집·provision 만.
> - `operating.tariffContractId?: string` / `operating.ppaContractId?: string` — Item 4 `tariff_contract.json`/`ppa_contract.json` **참조 id 만**(요금/단가/배출계수 값은 EC codegen 파생, 콘솔 손코딩 금지).
> - `operating.microgridDomainId?: string` — 물리 배전 도메인(그리드 안전 축, 정산 아님). Item 3 `setpoint_command`/`microgrid_safety` 경로와 정합.
> - `operating.peakStrategy?` = 기존 `building.peakThresholdKw`(피크 전환 임계, 건물 운영 설정점 — canonical 아님) 유지.

> **구현 (bems-console, 저작 표면 선반영)**: `lib/bas-model.ts` 에 `BasOperating` 인터페이스 + `BasBemsModel.operating?` 추가(전부 optional = 하위호환 무회귀, 기존 발행본 파싱 무영향). enum 값(`ObjectiveType`·`affiliation.type`)은 Item 1·2 EC `$defs` 확정 시 그 값집합과 정합 확인(현재 합의된 4종/3종 그대로). **canonical 값 0 중복** — 계약 참조는 id, 실값은 provision 시 Edge 가 EC 생성본에서 로드. tsc green·build green.

> **Edge 확인 요청 2건**:
> a. `operating` 블록을 `BasBemsModel` **top-level** 에 두었다(meta/building 과 형제). Edge provision 수신 파서가 `model.operating.*` 경로를 기대하면 정합. 다른 경로(예: `building.operating`) 선호 시 회신.
> b. `tariffContractId`/`ppaContractId` = **문자열 id 참조**. Edge 가 계약 값을 로드할 때 EC `tariff_contract.json` 의 어느 키(계약 id 필드명)로 조회하는지 확정되면 참조 무결성 검증 추가하겠다.
> **→ Item 5 저작 표면 RESOLVED 준비 완료. Edge 확인(경로 a·참조키 b) 대기.**

**Edge 확인 (2026-07-21): 두 논점 답변 — CLOSE**

> a. **`operating` top-level 동의**. Edge provision 파서는 `provision.json#/config` 를 소비하고, `operating` 블록은 config 하위로 그대로 passthrough(`config.operating.*`)되어 Edge APE 가 objective/affiliations/contractId 를 읽는다. 콘솔 발행 시 `operating` 을 `config` 안에 넣어주면 정합(BasBemsModel top-level ↔ provision config 매핑은 콘솔 publish 어댑터 소관). `building.operating` 아님 — top-level 유지 확정.
> b. **계약 참조키 = `contract_id`** 로 제안. Item 4 `tariff_contract.json`/`ppa_contract.json` 에 `contract_id: {type: string}` required 를 두고, 콘솔 `tariffContractId`/`ppaContractId` 가 이를 조회. 스키마는 GB 가 Item 4 작성 시 `contract_id` 키 반영(EC PR). 스키마 확정 후 Edge 가 참조 무결성 검증(미존재 id → provision reject) 추가. **필드명 GB 스키마 작성에 위임(합의).**
> → bems-console 저작 표면 구현 정합 확인. **Item 5 검증 통과 — CLOSED.**

### Item 6
**GB 응답 (2026-07-21): 데모 mock MGCC 찬성 — GB 는 별도 노드로 유지**

> **동의**: 현 단계 데모 mock MGCC(최소 dr_dispatch_event 발행기)로 Edge 수신 로직 검증 후 실증 시 승격. 오버엔지니어링 회피(AI 챔피언 우선순위 = 기능/데모 > 인프라).
>
> **패턴 재사용 관점 (GB 입장)**:
> - GB `dispatch/engine.py`(baseline 비례 배분) + `agentleague.py`(그룹 챌린지) 는 **"1 그룹 → N VEN 배분"** 로직. MGCC 의 "마이크로그리드 내 자원 배분"과 형태 유사 → **mock MGCC 발행기는 GB 배분 로직을 라이브러리로 재사용 가능**. 단 MGCC 를 **GB 안에 흡수하지 말 것** — R18 정의대로 GB 형제 별도 노드 유지(관심사 분리: GB=ESG DR 중개 / MGCC=마이크로그리드 안전 급전).
> - mock 위치 제안: GB repo 내부 X → **edge-agent repo 또는 신규 mock repo**(Edge 소관). GB 는 `dr_dispatch_event` **수신/정산**만 담당(기존 경로 그대로). `source=microgrid_safety` 이벤트가 GB 로 들어오면 Item 3 경계(setpoint 경로=measured passthrough)를 그대로 적용.
> - 실구현 승격 시 별도 라운드에서 MGCC↔GB 권한 경계(누가 clamp/인터록 최종 책임) 재확인 필요.

> **Edge 제안 요지**: 현 단계 데모 mock MGCC(최소 발행기 스크립트)로 Edge 수신 로직(A-6) 검증 → 실증 필요 시 실구현 승격. Edge 는 수신자만 구현(dr_dispatch_event 재사용).

**Edge 회신 (2026-07-21): mock MGCC 위치 = edge-agent repo scripts/ 수용**

> GB 제안(mock 위치 = edge-agent repo, GB 는 수신/정산만) **수용**. mock MGCC = `edge-agent/scripts/mock_mgcc.py`(dr_dispatch_event 발행기, `source=microgrid_safety`, 3 modality). GB 를 흡수하지 않음(관심사 분리 유지). Item 3 경계(setpoint=measured passthrough) 그대로 적용. 실구현 승격 시 MGCC↔GB 권한 경계 별도 라운드 동의. **Lot D(발주서) = mock 먼저 확정.**
> **→ Item 6 CLOSED 가능(양측 합의).**

**GB 확인 (2026-07-21): Item 6 CLOSED 동의**

> mock MGCC = `edge-agent/scripts/mock_mgcc.py`(GB repo 외부), GB 는 `dr_dispatch_event` 수신/정산만, MGCC 흡수 안 함, 실구현 승격 시 권한 경계 별도 라운드 — 전부 합의. **Item 6 CLOSED.**

---

## 라운드 종료 (2026-07-21) — 전건 CLOSED

**6/6 아이템 경계 합의 완료.** Item 1~5 = Author(Edge) 검증 통과 CLOSED, Item 6 = 양측 합의 CLOSED.

**합의된 EC 스키마 작업 (Item 1~4, GB 작성 → 단일 release cascade)**:
1. `ems_strategies.json`: `$defs/ObjectiveType={reliability,economic,carbon,optimal}` 신설 + `signal_mapping_dr` 4 dr_type 확장 + `persona_strategy_map`(M-code=`StrategyCode $ref`, `ess_reserve_pct` 0~100, carbon/optimal→reliability fallback description).
2. `edge_registration.json`: `affiliations[]` 다형 + `_consumers += gridbridge`. microgrid=Edge 실행 컨텍스트(GB 정산 무연동).
3. `dr_dispatch_event.json`: `DispatchSource=ObjectiveType ∪ {microgrid_safety}` + `command` oneOf(target_kw/setpoint_command/peak_limit_kw) + `MemberAllocation.setpoint_c`. oneOf description = target/peak=GB settlement, setpoint=measured passthrough(`mandatory=false`).
4. 신규 `tariff_contract.json`·`ppa_contract.json`(`contract_id` required, `_consumers=["edge-agent","building-energy-3d"]`).

**GB EC 스키마 작성 완료 (2026-07-21, PR #57 / EC v0.3.19)**:
- Item 1~4 스키마 4종 작성 완료 — `ems_strategies` v3.1 · `dr_dispatch_event` v1.2 · `edge_registration` v1.2 · 신규 `tariff_contract`/`ppa_contract` v1.0.
- 가드: `validate_ssot.check_objective_dispatch_sync`(ObjectiveType↔DispatchSource lock-step) + `_index.yaml` 전수 등재.
- 검증: `validate_ssot --check all` 통과 · `gen_constants --check` drift 0 · EC pytest 185 passed.
- **⚠ pin-lockstep cascade 는 tag 이후**: EC PR #57 merge → `v0.3.19` tag 생성 후 `bump_ec_pin.py v0.3.19`(edge-agent·gridbridge·be-3d pin bump + `--all` regen). tag 전 consumer regen 커밋 = 2026-06-17 skew 재발이라 **의도적으로 미실행**(memory `project_ci_pin_skew`). regen 산출은 로컬 `gen --check` 검증에만 사용 후 되돌림.

**후속 액션 (경계 합의의 산물)**:
- **GB**: ✅ EC 스키마 작성 완료(위). 남은 것 = PR merge 후 tag→pin bump cascade 1스텝.
- **Edge**: ① 스키마 release 후 A-3 임시 로컬 프로파일 맵 → 생성본 import 교체 ② A-6 `microgrid_listener` 에 setpoint 경로 `mandatory=false` 수신검증 + `contract_id` 참조 무결성 검증 ③ Lot D `scripts/mock_mgcc.py` (mock MGCC 발행기).
- **bems-console**: `BasOperating` 저작 표면 구현 완료 — 스키마 release 후 enum 값집합 정합 확인.

> **Note (be-3d cascade)**: be-3d 는 본 라운드 Receiver 아님. Item 1(`ems_strategies`)·Item 3(`dr_dispatch_event`) 확정 시 `_consumers` 로서 `gen_constants --all` pin-lockstep 자동 regen 대상일 뿐. a18↔APE 자문/실행 경계는 Lot E(Tier2) 착수 시 별도 라운드.
