# R18: Edge 자율 운영 정책 엔진(APE) — cross-repo 경계 합의 (Edge team)

**Date**: 2026-07-21
**Author**: Edge
**Receiver**: VW/GB, be-3d, bems-console

> **배경**: Edge-agent 에 OpenADR 경계 위 **자율 운영 감독 계층(APE = Autonomy Policy Engine)** 설계 완료 (구현 전). 설계 SSOT = `edge-agent/docs/DESIGN-AUTONOMOUS-PERSONA-AGENT.md`, 발주서 = `edge-agent/docs/WORK-ORDER-APE.md` (PR #50). 본 라운드 = 발주 Lot B(SSOT 스키마)·C(콘솔)·D(MGCC)의 **cross-repo 경계 합의**. Lot A(edge 내부 Tier1)는 외부 의존 0 이라 본 합의와 독립 착수.
>
> **명명 확정 (혼동 방지)**: "건물 자율 에이전트"·"BOPA" 폐기 → **APE(Edge Agent 의 기능)** + **운영 프로파일**(persona 아님). repo CLAUDE.md §Agent 어휘 4종(단독 "agent" 금지) 준수. be-3d **Specialist Agent**(a01~a25)와 별개 계층 — 혼동 방지 위해 각 팀 확인 요청.

## Items

| # | Severity | Issue | File(s) | Owner | State | Since |
|---|----------|-------|---------|-------|-------|-------|
| 1 | HIGH | `ems_strategies.json` 에 `persona_strategy_map` 추가 + `signal_mapping_dr` 에 `carbon`·`optimal` dr_type 확장(현 2종→4종). loader 검증 키 수 갱신 | ems_strategies.json | VW/GB | OPEN | 07-21 |
| 2 | HIGH | `edge_registration.json`: 단일 `group_id` → `affiliations[]` 다형(esg/microgrid/independent, 하위호환 마이그레이션). `_consumers` += gridbridge | edge_registration.json | VW/GB | OPEN | 07-21 |
| 3 | HIGH | `dr_dispatch_event.json`: `source` += `microgrid_safety`. `target_kw` → `command` oneOf(target_kw \| setpoint_command \| peak_limit_kw). `MemberAllocation` += `setpoint_c` | dr_dispatch_event.json | VW/GB | OPEN | 07-21 |
| 4 | MEDIUM | 신규 `tariff_contract.json`(계약전력·기본요금 고압A/B I/II/III·TOU) + `ppa_contract.json`(단가·탄소·take-or-pay). a23_tariff SSOT 정합 | (신규 2종) | VW/GB | OPEN | 07-21 |
| 5 | MEDIUM | 역할 경계 확인: be-3d `a18_preference`(클라우드 자문·Pareto) vs Edge APE(엣지 상시 실행) — 중복 아닌 자문/실행 분리 합의. Lot E(Tier2)에서 a18 Gateway 노출을 Edge 가 캐싱·소비하는 계약 | (문서 경계) | be-3d | OPEN | 07-21 |
| 6 | MEDIUM | 저작 경계 확인: `BasBemsModel` 에 operating_profile·tariff·ppa·affiliations·microgrid 필드 추가 협의. 콘솔=저작(편집 UI), Edge=실행(provision 수신). 1노드=1VEN=1Edge=1BAS/BEMS 재확인 | BasBemsModel (bems-console) | bems-console | OPEN | 07-21 |
| 7 | LOW | MGCC(마이크로그리드 중앙 컨트롤러) 실체 결정 — GB 형제 별도 노드. 실구현 vs 데모 mock. GB `agentleague.py`/`dispatch/engine` 패턴 재사용 가능성 검토 | (신규 repo 후보) | VW/GB | OPEN | 07-21 |

---

## Responses

### Item 1
_(대기 — Receiver: VW/GB)_

> **Edge 제안 요지**: `persona_strategy_map` = `{profile(economic/reliability/optimal/carbon) → {상시 M-code, 피크 M-code, ess_reserve_pct}}`. `signal_mapping_dr` 는 이미 `{reliability, economic}` 골격 존재 → `carbon`·`optimal` 만 추가하면 4 dr_type. codegen(`gen_constants --all`) = 6-repo pin-lockstep 이므로 Item 1~4 를 **단일 EC release 로 묶기** 권장(drift gate 안전).

### Item 2
_(대기 — Receiver: VW/GB)_

> **Edge 제안 요지**: 기존 `group_id` 는 `affiliations[type=esg]` 로 자동 마이그레이션(무회귀). GB 가 `dr_venues` 에서 소속을 어떻게 표현할지(단일 vs 다중) 정합 필요 — Edge 는 다중 소속(ESG+마이크로그리드 동시) 지원.

### Item 3
_(대기 — Receiver: VW/GB)_

> **Edge 제안 요지**: `setpoint_command` = `{setpoint_c, ramp, zone?}` — MGCC 가 절감량(what) 대신 설정온도(how) 직접 지정. 단 Edge 는 인터록·critics_gate 통과 후 clamp(그리드 안전 < 사람 안전). 기존 `target_kw` 경로 하위호환 보존.

### Item 4
_(대기 — Receiver: VW/GB)_

> **Edge 제안 요지**: canonical 값(KEPCO 2026 요금·배출계수 0.4173·KAU)은 생성본 import(손편집 금지). take-or-pay 최소인수는 비용최적 목적함수의 하한 제약.

### Item 5
_(대기 — Receiver: be-3d)_

> **Edge 제안 요지**: a18 = Layer1 클라우드 *자문*(Pareto front 산출), APE = Layer3 엣지 *상시 실행*. Lot E 에서 Edge 가 a18 Pareto 를 Gateway 로 pull → 캐싱 → 오프라인 fallback. 계약 = a18 출력 스키마(front 점들)를 Edge 가 소비 가능한 형태로. 중복 구현 아님 확인 요청.

### Item 6
_(대기 — Receiver: bems-console)_

> **Edge 제안 요지**: `EDGE_TERMINOLOGY_AND_POINT_MODEL.md`(2026-06-30) 의 1노드=1VEN=1Edge=1BAS/BEMS 재확인. 운영 프로파일·계약·소속은 `BasBemsModel` 의 일부로 콘솔에서 편집 → publish → Edge provision 수신. 편집 UI 는 콘솔 소관, Edge 는 수신·실행만. 필드 스키마 협의.

### Item 7
_(대기 — Receiver: VW/GB)_

> **Edge 제안 요지**: 현 단계 데모 mock MGCC(최소 발행기 스크립트)로 Edge 수신 로직(A-6) 검증 → 실증 필요 시 실구현 승격. Edge 는 수신자만 구현(dr_dispatch_event 재사용).
