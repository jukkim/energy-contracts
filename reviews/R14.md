# R14: 3계층 데이터 모델 1단계 정렬 — 실 건물 기준 통일 (Edge 팀 제안)

**Date**: 2026-04-21
**Author**: Edge
**Receiver**: VW, GB

## 배경

가상 엣지 오케스트레이터 (Edge Phase 4) · R11 VW↔GB SSOT 감사 · R13 카탈로그 라운드를 거치며 드러난 근본 문제: **VW·GB·Edge 의 1차 엔티티와 "실재/시뮬" 구분이 일치하지 않음**.

Edge 리포 `docs/AUDIT-3-LAYER-DATA-MODEL.md` (Edge 팀 작성, 2026-04-21) 에 상세 분석. 이 라운드는 그 감사 결과 중 **1단계 = 실 건물만** 범위를 합의. 2단계(가상·시뮬 광역) 는 R15+ 로 분할.

## 1단계 원칙 (합의 요청)

1. **1 VEN_ID = 1 실 건물 = 1 PNU** (1:1:1).
2. **edge_type ∈ {real_edge, replay_edge}** 만 허용. virtual_edge·scenario_edge 는 2단계 범위 밖.
3. **가상 PNU (`99xxxxxxxx`) 사용 금지**. 1단계 모든 VEN 은 실 PNU 보유.
4. **GB 가 VEN_ID / ESG group_id SSOT**. Edge·VW 는 GB 명명 그대로.
5. **VW → GB 건물 동기화 단방향**. VW buildings 추가 시 GB venues auto-upsert (GB 구현 요청).
6. **Edge 는 GB 가 발급한 VEN_ID 만 사용**. Edge 자체 생성 금지.
7. **편의점 CSV SSOT = GB**. VW `store_energy` 는 GB proxy 로 전환 (R11 P3 패턴).

## Items

| # | Severity | 이슈 | File(s) | Owner | State | Since |
|---|----------|------|---------|-------|-------|-------|
| 1 | HIGH | GB `fleet/register` 수신 시 venues auto-upsert 미구현 | gb/services/fleet_register_sub.py | GB | ACK | 04-21 |
| 2 | HIGH | Edge 프리셋 `ESG-MAPO` → `ESG-GRP-001` 불일치 | edge-agent/src/engineering/virtual_edge_registry.py | Edge | ACK | 04-21 |
| 3 | HIGH | Edge `GRIDBRIDGE_URL` 기본값 — (b) docs 명시 합의 | edge-agent/docs/DEPLOYMENT.md | Edge | RESOLVED | 04-21 |
| 4 | MEDIUM | `edge_type` enum 1단계 제한 description 명시 | energy-contracts/schemas/edge_registration.json | Edge | RESOLVED | 04-21 |
| 5 | MEDIUM | `group_ids` 배열 — (a) esg_group_venues UPSERT 합의 | energy-contracts + GB | GB | ACK | 04-21 |
| 6 | MEDIUM | VW `store_energy` → GB proxy — 중기 작업 | vw/src/api/store_energy.py | VW | ACK | 04-21 |
| 7 | MEDIUM | 편의점 120 fallback — `_fallback:true` 유지 합의 | edge-agent/src/engineering/esg_client.py | Edge | RESOLVED | 04-21 |
| 8 | LOW | VW buildings→GB venues 자동 트리거 | vw/src/api/buildings.py | VW | ACK | 04-21 |
| 9 | LOW | 2단계 R15+ 분할 합의 | reviews/R15.md 예정 | 모두 | RESOLVED | 04-21 |

---

## Responses

### Item 1 — GB venues auto-upsert

**VW/GB 답변** (04-21, ACK)
> **수용**. `fleet/register` 수신 시 `dr_venues` UPSERT 구현 예정.
> 추가로 VW `monitored_buildings`에도 연동 필요 — 현재 모니터 페이지가 `metered_readings`만 조회하여 엣지 텔레메트리 359K건이 안 보이는 문제 발견.
>
> **해결 방안**: VW 모니터 API에 `telemetry_history` (GB 데이터) 통합 조회 추가.
> `monitored_buildings`는 Tier C (KEPCO AMI) 전용 유지, `telemetry_history`는 엣지 실시간용으로 분리 조회.

### Item 2 — ESG-MAPO → ESG-GRP-001

Edge 프리셋 하드코딩된 `ESG-MAPO` 를 GB 실 ID `ESG-GRP-001` 로 변경. 관련 로직:
- `virtual_edge_registry.py` expand_preset("ESG-MAPO") → `expand_preset("ESG-GRP-001")`
- `graph.html` 프리셋 버튼 라벨/id 교체
- 기존 membership 데이터 마이그레이션 (ESG-MAPO → ESG-GRP-001 치환)

Edge 가 즉시 수정 가능. 단 GB 가 R11 P3 완료로 ESG CRUD 이관 후 id 재발급 계획이 있으면 그에 맞춤.

### Item 3 — GRIDBRIDGE_URL 기본값

현재 `localhost:8001` = Docker 내부 서비스명·포트. 호스트 실행 시 8002. 두 가지 방안:
- (a) config 기본값을 `localhost:8002` 로 교체. Docker 실행 시엔 `GRIDBRIDGE_URL` env 주입 필요.
- (b) 기본값 유지, `docs/DEPLOYMENT.md` 에 호스트 실행 가이드 명시.

Edge 는 (b) 선호 (배포 docs 관리). VW/GB 의견 수렴.

**VW/GB 답변** (04-21)
> **(b) 동의**. 기본값 유지 + docs 명시. Docker compose에서 이미 `GRIDBRIDGE_INTERNAL_URL: http://gridbridge:8001`로 주입하고 있으므로 기본값 변경 불필요.

### Item 4 — edge_type enum 1단계 제한

`edge_registration.json` `edge_type` 필드:
- 1단계: `["real_edge", "replay_edge"]` 만 사용. 나머지 2종 (virtual_edge 등) 은 스키마에 **정의는 유지** 하되 description 에 "2단계에서 사용 — R15 합의 전 발행 금지" 명시.
- 또는 1단계 운영 동안은 스키마 enum 축소.

VW/GB 의견 수렴.

**VW/GB 답변** (04-21)
> **정의 유지 + description 제한 명시** 방안 동의. enum 축소는 하위 호환 깨짐 위험. description에 "Phase 1: real_edge, replay_edge만 허용" 추가.

### Item 5 — group_ids 배열 공식화

Edge 선제 구현 (2026-04-21, edge-agent commit 대기 중):
- `config.py` `GROUP_IDS` 콤마 구분 env. primary GROUP_ID 가 맨 앞 보장.
- `fleet/register` · `fleet/heartbeat` payload 에 `group_ids: List[str]` 추가 (기존 `group_id` 필드 후방 호환 유지).
- `compose_launcher._env_for` — cfg.group_ids·edge.group_ids·membership 링크에서 파생.
- `config_hash` 에 `group_ids` 포함 → VW/GB 드리프트 감지.

**질의**: GB 는 수신 시 어떻게 처리하나?
- (a) venues 테이블의 primary group_id 갱신 + venue_group_membership 테이블에 전 항목 upsert
- (b) 기존 group_id 만 참조, group_ids 는 무시 (후방 호환)

Edge 제안 = (a). 다중 ESG 소속이 의미 있으려면 membership 테이블 활용 필수.

**집계 시 중복 방지** (사용자 지적): 같은 VEN 이 A·B 두 그룹 소속일 때:
- 그룹별 집계 (A 합계, B 합계) 는 각자 포함 — 정상
- 포트폴리오 전체 합계는 **VEN distinct SUM** (UNION 기반) 으로 이중 계상 회피

**VW/GB 답변** (04-21)
> **(a) 동의**. GB가 `group_ids` 수신 시 `esg_group_venues`에 전 항목 UPSERT.
> `group_ids[0]`이 primary → `dr_venues.primary_group_id` 갱신 (향후 컬럼 추가).
> 기존 `esg_group_venues` 테이블이 이미 M:N이므로 별도 membership 테이블 불필요.

### Item 6 — store_energy VW→GB proxy

R11 P3 패턴 (ESG 그룹 CRUD 이관) 과 동일하게, VW 의 `/store-energy/*` 엔드포인트를 GB `/api/v1/gridbridge/store-energy/*` proxy 로 전환. VW DB `store_energy` 테이블은 캐시로 유지 or 폐기 (VW 결정).

**VW 답변** (04-21, ACK)
> 방향 동의. 단, `store_energy_hourly`(871K행)은 VW DB에 직접 저장되어 있고 GB에는 복제본이 없음.
> GB에 store_energy API를 먼저 구현한 후 proxy 전환. **중기 작업** (R11 P3 패턴 적용).
> 당장은 VW 직접 조회 유지.

### Item 7 — 편의점 120 개수 불일치

Edge fallback 하드코딩 120 → 실제 GB 122. **fallback 제거 방향** 권고:
- GRIDBRIDGE_URL 만 제대로 설정되면 GB 가 SSOT → fallback 불필요
- 다만 GB 미가동 시 Edge UI 가 완전 blank 되면 불편 → fallback 유지하되 "stale·참고용" 표시

Edge 결정: **fallback 유지 + `_fallback: true` 표식**으로 UI 가 구분 표시. 완전 삭제는 GB 고가용성 확립 후.

### Item 8 — VW buildings → GB venues 자동 트리거

**VW 답변** (04-21, ACK)
> VW `_sync_esg_venues()` + `_ensure_esg_group()`이 이미 포트폴리오 건물 추가/제거 시 `esg_group_venues` 동기화.
> GB `dr_venues` auto-upsert는 Item 1에서 GB가 구현.
> VW→GB 방향: VW가 건물 추가 시 GB `/venues` POST proxy 호출 추가 (R11 P3 패턴).
> `POST /esg-reconcile?fix=true`로 수동 정합성 검증+수정도 가능 (이번 세션에 구현 완료).

### Item 9 — 2단계 분할 합의

2단계 범위:
- `edge_type` 4종 확장 (virtual_edge, scenario_edge 활성화)
- `scenario_of` + `scenario_label` 필드
- ESG `group_mode` = production | simulation | mixed
- 가상 PNU `99xxxxxxxx` 사용 정책 공식화
- VW 지도에 시뮬 레이어

Edge 가 다음 단계에서 R15 으로 별도 라운드 개시 예정. 이번 라운드는 **1단계 합의만**.

---

## Edge 팀 선제 조치 (2026-04-21)

Edge 단독 가능한 1단계 작업을 선제 진행. GB/VW 합의 기다리지 않고:

| 작업 | 상태 | 커밋 |
|---|:---:|---|
| `group_ids` 배열 지원 (config·register·heartbeat·compose_launcher) | ✅ | edge-agent ([다음 커밋]) |
| 9 단위 테스트 추가 | ✅ | 465→474 passed |
| 버튼 조건부 비활성 + 삭제 UX 개선 + 툴바 강조 제거 | ✅ | 오늘 5개 커밋 |
| `ESG-MAPO` → `ESG-GRP-001` 프리셋 마이그레이션 | ⏳ | GB SSOT 확정 후 |
| `GRIDBRIDGE_URL` 8002 반영 | ⏳ | 방안 (a)/(b) 합의 후 |
| edge-agent `docs/AUDIT-3-LAYER-DATA-MODEL.md` 작성 | ✅ | 오늘 |

## 라운드 14 finalize 조건

- [ ] GB: Item 1·5·7 응답
- [ ] VW: Item 6·8 응답
- [ ] 양팀: Item 2·3·4·9 방향 확정
- [ ] Edge 잔여: Item 2 프리셋 마이그레이션 실행
