# R17: 3-Phase 통합 테스트 계획 — Edge 협조 요청 (VW/GB 제안)

**Date**: 2026-04-21
**Author**: VW/GB
**Receiver**: Edge

## 배경

`docs/TEST_PLAN.md` (471행) 작성 완료. 3 Phase(기능/호환성/성능) 중 Edge 참여가 필요한 항목을 협의한다.

## Items

| # | Severity | 이슈 | Owner | State | Since |
|---|----------|------|-------|-------|-------|
| 1 | HIGH | Phase 2 호환성: Edge가 ESG 그룹 VEN에 telemetry 전송 시 GB+VW 양쪽에서 동일 데이터 표시되는지 검증. Edge 측 검증 스크립트 필요 | Edge | RESOLVED | 04-21 |
| 2 | HIGH | Phase 3 성능: Edge 50~500 VEN 동시 전송 시 부하 생성 방법 합의. `compose_smoke.py` 활용 vs 별도 부하 생성기 | Edge | RESOLVED | 04-21 |
| 3 | MEDIUM | Phase 2 호환성: fleet/register → auto-upsert → ESG 그룹 할당 자동 검증. Edge가 새 VEN으로 register 발행 → GB+VW 반영 확인 | Edge | RESOLVED | 04-21 |
| 4 | MEDIUM | Phase 2 호환성: 데모 리셋 E2E — GB reset API → MQTT → Edge DemoResetListener → ReplayDriver 초기화 → 재전송 시작 확인 | Edge | RESOLVED | 04-21 |
| 5 | MEDIUM | Phase 3 성능: Edge 컨테이너 리소스 제한 합의. 1 VEN당 메모리/CPU 할당량, 동시 500 VEN 시 호스트 리소스 예산 | Edge | RESOLVED | 04-21 |
| 6 | LOW | Phase 1 기능: Edge Engineering UI (graph.html, fleet.html) 자체 테스트 범위. Edge가 자체 Playwright 시나리오 보유 여부 | Edge | RESOLVED | 04-21 |

---

## VW/GB 제안

### Item 1 — 호환성 검증 스크립트

Edge에서 테스트그룹1(ESG-PF-7) VEN 5개에 데이터 전송 후:
```python
# 검증 포인트
assert GB telemetry_history.count(ven_id, mode='demo') > 0
assert GB /mqtt/telemetry 응답에 ven_id 포함
assert VW /monitor/buildings 응답에 ven_id 포함
assert VW /portfolios/{pid}/store-energy 응답에 데이터 존재
```
Edge가 전송 후 자체 검증하거나, VW/GB가 Playwright로 검증하거나, 양쪽 병행 가능.

### Item 2 — 부하 생성 방법

**옵션 A**: `compose_smoke.py --count 200 --replay-speed 720` (기존 Edge 도구)
**옵션 B**: 별도 paho-mqtt 스크립트로 GB에 직접 MQTT publish (Edge 컨테이너 없이)
**옵션 C**: A+B 병행 — 실제 Edge 100 + 직접 MQTT 400

VW/GB 제안: **옵션 A** (실제 Edge 경유가 성능 테스트로서 의미 있음). 500 VEN이 호스트 리소스 초과 시 옵션 C로 전환.

### Item 3 — VEN auto-register 검증

```
테스트: 새 VEN-TEST-R17-001 생성
  Edge: fleet/register 발행 (group_ids=["ESG-PF-7"])
  검증: GB /venues/VEN-TEST-R17-001 → 200
  검증: GB /esg/groups/ESG-PF-7 → venues에 포함
  정리: 테스트 후 VEN 삭제
```

### Item 5 — 리소스 예산

현재 서버: Windows 11, RTX 4090, RAM 64GB (추정)
```
Docker 현재 할당:
  API:        8GB / 4 CPU
  Worker:     8GB / 4 CPU
  GridBridge: 1GB / 1 CPU
  DB:         4GB
  Redis:      2GB
  MQTT:       0.5GB

Edge 500 VEN 시 추가:
  옵션 A: 500 컨테이너 × ~50MB = ~25GB 필요 → 호스트 초과 가능
  옵션 C: 100 컨테이너(5GB) + 400 직접 MQTT → 호스트 내 가능
```

Edge 측 `compose_smoke.py`의 1 컨테이너당 리소스 사용량 확인 요청.

---

## Finalize 조건

- [ ] Edge: Item 1 검증 스크립트 방법 합의
- [ ] Edge: Item 2 부하 생성 옵션 선택 (A/B/C)
- [ ] Edge: Item 5 컨테이너 리소스 사용량 보고
- [ ] Edge: Item 6 자체 UI 테스트 범위 보고

---

## Edge 팀 응답 (2026-04-21 14:20)

### Item 1 — 호환성 검증 스크립트 (RESOLVED)

Edge 측 통합 검증 스크립트 신설: **`scripts/e2e_integration.py`** (edge-agent commit `e80e5fb`). 4 서브커맨드:

| 명령 | 설명 |
|---|---|
| `verify <group_id>` | ESG 그룹 VEN 의 GB ESG/GB venues/Edge Fleet Monitor 3계층 일치 검증 |
| `register <ven_id> --group <gid>` | fleet/register 발행 + GB dr_venues 등록 10초 내 확인 |
| `reset --scope <all\|ven\|group>` | GB POST /demo/reset + MQTT publish 확인 |
| `stats` | edge-ven-* 컨테이너의 RSS/CPU 리포트 |

**ESG-PF-7 실행 결과 (2026-04-21 14:22)**:
```
GB 그룹 ESG-PF-7: 5 venues
VEN-STORE-B008/B009/B030/B061/RGR010533 — ESG 멤버 ✓ · GB venue ✓
결과: PASS
```

### Item 2 — 부하 생성 방법 (RESOLVED, 옵션 A 수용)

**옵션 A** 수용. `scripts/compose_smoke.py` 가 이미 224채까지 검증된 도구.
- 224채 × 60분 실측: 100% tel·heartbeat, RSS 평균 60 MB/container
- 500 VEN 도달 시 호스트 RAM 여유 확인 후 A 유지 or C 전환
- 옵션 B (직접 MQTT publish) 는 Edge 체인을 건너뛰어 성능 테스트 본질 훼손 → 비추천

### Item 3 — VEN auto-register 검증 (RESOLVED, 이미 검증)

R14 Item 1 (2026-04-21 13:15) 에서 이미 GB `_auto_register_ven` 5/5 검증 완료. 자동화는 `scripts/e2e_integration.py register <ven>` 으로 단일 명령.

### Item 4 — 데모 리셋 E2E (RESOLVED, 이미 완료)

R15 E2E (edge-agent `8f0d32a`): GB POST → MQTT → Edge DemoResetListener → ReplayDriver.reset(). `scripts/e2e_integration.py reset --scope all` 로 재현.

### Item 5 — 컨테이너 리소스 실측 보고 (RESOLVED)

**`docker stats --no-stream` 실측 (5 컨테이너, ESG-PF-7, idle 상태)**:

| 컨테이너 | RSS | CPU |
|---|---|---|
| edge-ven-store-b008 | 43.24 MiB | 0.04% |
| edge-ven-store-b009 | 43.23 MiB | 0.05% |
| edge-ven-store-b030 | 43.21 MiB | 0.04% |
| edge-ven-store-b061 | 43.23 MiB | 0.04% |
| edge-ven-store-rgr010533 | 40 MiB | 0.04% |

**평균 42.6 MB/container**. 대규모 fleet 실측 (이전 세션):
- 10채 32 MB, 50채 32 MB, 124채 80.7 MB, 224채 60.3 MB (공유 라이브러리 로딩 효과)

**500 VEN × 50~70 MB ≈ 25~35 GB** — 호스트 64GB 에서 GB/VW/DB/Redis/MQTT 약 20GB 차감 후에도 **옵션 A 가능**.

### Item 6 — UI 자체 Playwright regression (ACK)

현재 **미구축**. 오늘 세션의 graph.html/fleet.html Playwright 시나리오는 수동 시연용. 자동 regression 으로 전환은 다음 작업 후보 (우선순위 낮음).

제안 범위 (중기):
- graph.html: 프리셋 로드 → 연결 → 기동 → 정지 → 초기화 골든 경로
- fleet.html: 5 VEN 수신 → 상세 패널 → Group 필터 → 정렬
- engineering/index.html: wizard 5단계 Seal 흐름

---

## Finalize 조건 (갱신)

- [x] Edge: Item 1 검증 스크립트 (`scripts/e2e_integration.py`)
- [x] Edge: Item 2 부하 생성 옵션 A 수용
- [x] Edge: Item 5 컨테이너 리소스 실측 (42.6 MB/container)
- [x] Edge: Item 6 UI regression — VW/GB가 25개 Playwright 테스트 제공 (05-16, edge-agent `c05d33c`)

---

## VW/GB Follow-up (2026-05-16)

### Item 6 — Edge UI Playwright regression (RESOLVED)

**VW/GB가 직접 구현** (2026-05-16, edge-agent `c05d33c`):

`tests/browser/` 디렉토리에 25개 Playwright 골든 경로 테스트 작성:

| 파일 | 테스트 수 | 범위 |
|------|:---------:|------|
| `test_fleet_golden.py` | 10 | 헤더/뱃지/툴바/검색/정렬/stale 기본값/그룹 필터/빈 상태/상세 패널 |
| `test_graph_golden.py` | 6 | 헤더/캔버스/툴바/프리셋 노드·링크 검증/초기화 |
| `test_wizard_golden.py` | 9 | 헤더/스텝바/JS 에러 0/meta·capabilities·config·sessions·esg-groups API/dry-run |

**인프라**: `conftest.py`에 uvicorn subprocess 서버 픽스처 (TEST_MODE=true, 랜덤 포트) + `seed_preset` ESG-GRP-001 자동 시딩.

Edge 팀은 `pytest tests/browser/ --headed`로 실행 가능. `pytest-playwright>=0.5` dev 의존성 추가됨.

**Finalize 요청**: 6건 전부 RESOLVED. R17 CLOSED 전이 가능.

---

## Edge 팀 확인 (2026-05-16)

25개 Playwright 테스트 확인 완료 (`pytest tests/browser/ --co -q` → 25 collected):
- `test_fleet_golden.py` 10개 ✓
- `test_graph_golden.py` 6개 ✓
- `test_wizard_golden.py` 9개 ✓

`conftest.py` uvicorn 픽스처 + `seed_preset` 시딩 구조 확인.

**6건 전부 RESOLVED 인정. VW/GB Author CLOSED 전이 요청.**
