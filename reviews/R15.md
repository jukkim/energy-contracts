# R15: 데모 모드 데이터 관리 — 편의점 리플레이 + 리셋 프로토콜 (VW/GB 제안)

**Date**: 2026-04-21
**Author**: VW/GB
**Receiver**: Edge, VW, GB (3계층 공동)

## 배경

편의점 220개의 실측 데이터(2022/2024)가 VW DB에 존재. 데모 시 이 데이터를 2026년으로 시간 이동(time-shift)하여 Edge → GB → VW로 전송하면 실시간 운영처럼 보여줄 수 있다.

단, 원본 데이터 보존 + 데모 데이터 리셋이 보장되어야 한다.

## 요구사항

1. **원본 보존**: 편의점 220개의 2022/2024 `store_energy_hourly` 데이터는 삭제 불가
2. **시간 이동**: 데모 시 과거 데이터의 timestamp를 2026년으로 변환하여 전송
3. **리셋**: 데모 데이터를 언제든 초기화 가능 — GB + Edge 동시 리셋
4. **구분**: 원본 vs 데모 데이터가 명확히 구분되어야 함 (기존 `mode='demo'` 활용)

## Items

| # | Severity | 이슈 | 영향 | Owner | State | Since |
|---|----------|------|------|-------|-------|-------|
| 1 | HIGH | 데모 리셋 프로토콜 — GB API 구현 완료 (`cb57c96`) | GB API + MQTT | Edge | CLOSED | 04-21 |
| 2 | HIGH | time-shift 방식 — 옵션 B (상대 이동) 합의 | Edge 구현 | - | CLOSED | 04-21 |
| 3 | MEDIUM | 리셋 범위 — scope(all/ven/group) API 구현 완료 | GB API | Edge | CLOSED | 04-21 |
| 4 | MEDIUM | 원본 보호 — 테이블 물리 분리로 충분 합의 | VW DB | - | CLOSED | 04-21 |
| 5 | MEDIUM | 데모 초기화 UI — GB 랜딩 "데모 리셋" 버튼 구현 완료 | GB static | Edge | CLOSED | 04-21 |
| 6 | LOW | 리플레이 속도 — REPLAY_SPEED 기본 360 합의 | Edge config | - | CLOSED | 04-21 |
| 7 | LOW | MQTT 토픽 — mqtt-topics.md 업데이트 완료 (`bed37ea`) | energy-contracts | - | CLOSED | 04-21 |

---

## VW/GB 제안

### Item 1 — 데모 리셋 프로토콜

```
리셋 흐름:
  관리자 → POST /api/v1/gridbridge/demo/reset (GB API)
                    ↓
  GB: DELETE FROM telemetry_history WHERE mode = 'demo'
  GB: MQTT publish → fleet/demo/reset/all (또는 /{ven_id})
                    ↓
  Edge: 수신 → 리플레이 중단 + 내부 상태 초기화
                    ↓
  VW: 모니터 캐시 무효화 (자동 — 5분 TTL)
```

**핵심**: `mode='demo'`만 삭제하므로 `mode='live'` 실시간 데이터는 영향 없음.

### Item 2 — time-shift 방식

**VW/GB 제안**: 현재 시각 기준 상대 이동 (옵션 B)

```
옵션 A (단순 이동):
  2022-01-01 00:00 → 2026-01-01 00:00 (+4년)
  문제: 리플레이 시작 시점에 따라 "미래" 데이터가 한꺼번에 들어옴

옵션 B (상대 이동) ← 권장:
  데모 시작 시각 = NOW()
  원본 데이터 첫 레코드 시각 = T0
  변환: new_ts = NOW() + (original_ts - T0)
  효과: 데모 시작부터 실시간처럼 한 건씩 전송
```

### Item 3 — 리셋 범위

**VW/GB 제안**: 기본은 `mode='demo'` 전체 삭제, 옵션으로 VEN별 선택

```sql
-- 전체 리셋
DELETE FROM telemetry_history WHERE mode = 'demo';

-- VEN별 리셋 (선택)
DELETE FROM telemetry_history WHERE mode = 'demo' AND ven_id = :vid;
```

### Item 4 — 원본 보호

`store_energy_hourly`는 기존 데이터로 건드리지 않음. 데모 데이터는 `telemetry_history`에만 적재 (mode='demo'). 두 테이블이 물리적으로 분리되어 있으므로 추가 보호 불필요.

### Item 5 — 데모 초기화 UI

GB 랜딩에 "데모 리셋" 버튼 추가:
```html
<button onclick="resetDemo()">데모 데이터 초기화</button>
```
→ `POST /api/v1/gridbridge/demo/reset` (admin auth 필요)

### Item 6 — 리플레이 속도

Edge `REPLAY_SPEED_MULTIPLIER` 환경변수 (기본 360 = 1시간→10초)

### Item 7 — 리셋 MQTT 토픽

```
fleet/demo/reset/all       — 전체 VEN 리셋 (GB publish, Edge subscribe)
fleet/demo/reset/{ven_id}  — 특정 VEN 리셋
```
QoS 2, retain=No. 페이로드: `{"action":"reset","timestamp":"ISO8601","reason":"manual"}`

---

## VW/GB 응답 (2026-04-21, Edge 질의 답변)

### Item 1 질의 — 리셋 시 큐 폐기

> **(a) 전부 폐기** 동의. 깨끗한 리셋이 일관적.
> GB 측: `DELETE WHERE mode='demo'` 후 `fleet/demo/reset/all` 발행.
> Edge 측: 큐 폐기 + ReplayDriver 리셋.
> 리셋 이후 Edge가 처음부터 재생 → GB에 새 demo 데이터 축적.

### Item 3 — 리셋 범위 확정

> **기본: `mode='demo'` 전체 삭제 + 옵션으로 VEN별/그룹별 선택.**
> 페이로드의 `scope` + `target_id` 필드 (Edge 제안) 수용:
> ```json
> {"action":"reset","scope":"all","timestamp":"ISO8601","reason":"manual"}
> {"action":"reset","scope":"ven","target_id":"VEN-STORE-001","timestamp":"..."}
> {"action":"reset","scope":"group","target_id":"ESG-STORE-100","timestamp":"..."}
> ```

### Item 4 — 원본 보호 확정

> 테이블 물리 분리로 충분. 추가 플래그 불필요.
> `store_energy_hourly` = 원본 (220개 × 2022/2024), `telemetry_history` = 데모+라이브.

### Item 5 — UI 구현 계획

> GB 랜딩 + VW 관리자 양쪽에 "데모 리셋" 버튼 추가 예정.
> GB 리셋 API 구현 시 함께 작업.

### Item 7 — scope 필드 수용

> Edge 추가 제안 `scope ∈ {all|ven|group}` + `target_id` 수용.
> `mqtt-topics.md` 업데이트 시 반영.

## Finalize 조건

- [x] Edge: Item 2 time-shift 방식 합의 (B 수용)
- [x] Edge: Item 6 리플레이 속도 합의 (REPLAY_SPEED 기본 360)
- [x] Edge: Item 1 큐 폐기 합의 (a)
- [x] 모두: Item 7 MQTT 토픽 합의 + mqtt-topics.md 업데이트 (`bed37ea`)
- [x] VW: Item 4 원본 보호 확정
- [x] GB: Item 1·3 리셋 API 구현 (`POST /demo/reset` + `GET /demo/status`, 커밋 `cb57c96`)
- [x] GB: Item 5 GB 랜딩 "데모 리셋" 버튼 구현
- [x] Edge: Item 1·3·5 검증 → CLOSED 전이 (2026-04-21 13:30 E2E 확인)
- [x] Edge: `DemoResetListener` 구현 (fleet/demo/reset 구독) — main.py 배선 + queue.purge()

---

## Edge 팀 응답 (2026-04-21)

### Item 2 — time-shift 방식 (RESOLVED)

**옵션 B (상대 이동) 수용.** 이유:

- 옵션 A 의 "리플레이 시작 시 과거 4개월치 한꺼번에 들어옴" 문제는 현재 ReplayDriver `_current_sim_time()` 이 `start_wall + elapsed × speed` 로 계산하므로 과거 한꺼번에 flush 는 발생하지 않으나, B 가 의미상 더 명확 (데모 시작 시각 = NOW() 기준).
- 기존 `ReplayDriver(start_sim_ts=NOW())` 인자로 B 를 **현행 구현에서 바로 구현 가능**. `year_shift_to` 필드는 deprecated 예정.
- Edge 측 env 신설: `REPLAY_MODE ∈ {wallclock|shift}` (기본 `wallclock` = B). `shift` 는 호환성 위해 유지.
- publish 시 `timestamp` 필드에 현재 wall clock + (row_ts - first_ts) 가 찍힘 → GB 에 실시간 데이터로 보임.

### Item 6 — 리플레이 속도 (RESOLVED, env 이름 통일 요청)

**합의**: 기본 가속 배율 조정 가능. 단 **env 이름은 기존 `REPLAY_SPEED` 유지** 권고 (GB 제안의 `REPLAY_SPEED_MULTIPLIER` 는 기존과 중복).

- 현재 Edge `REPLAY_SPEED` 기본 60 (1분 → 1초).
- VW/GB 가 제안한 360 (1시간 → 10초) 은 편의점 hourly 데이터에 더 적합 → **기본값을 360 으로 상향 권장**.
- 컨테이너 기동 시 override 가능: `REPLAY_SPEED=3600` 등.

**현재 상태**: `config.py` 와 `ReplayDriver(speed=...)` 이미 구현. 기본값만 조정하면 됨.

### Item 1 — 데모 리셋 프로토콜 (ACK, 구현 대기)

흐름 전체 동의. Edge 측 구현 계획:
- `fleet/demo/reset/all` + `fleet/demo/reset/{ven_id}` 구독자 `DemoResetListener` 신설 (FleetMonitor/BundleNotifyListener 패턴).
- 수신 시:
  1. `ReplayDriver` 상태 초기화 (내부 `_start_wall = _now()` 로 리셋 → 처음부터 재생)
  2. 진행 중인 telemetry publish loop 중단 → 리셋 완료 후 재개
  3. 선택: `publisher._queue` drain 여부 (리셋 = 큐 폐기 동의 필요)
- 구현은 **GB 리셋 API (Item 3) 확정 후** 착수. 독립 구현 가능하지만 GB 가 실제 publish 하지 않으면 무의미.

**질의**: 리셋 시 Edge `publisher._queue` 에 남은 메시지도 폐기할까요?
- (a) 전부 폐기 (깨끗한 리셋)
- (b) 유지 (큐에 있는 건 이미 publish 시도한 것이라 GB 재수신 시 필터링)

Edge 제안 = (a). GB 가 `mode='demo'` 만 삭제하므로 Edge 도 demo 큐만 폐기가 일관적.

### Item 7 — MQTT 토픽 (RESOLVED, Edge 측 수락)

제안한 토픽·QoS·retain 수용:
- `fleet/demo/reset/all` + `fleet/demo/reset/{ven_id}`
- QoS 2, retain=No
- 페이로드 `{"action":"reset","timestamp":"ISO8601","reason":"manual"}`

**추가 필드 제안**: `scope ∈ {all|ven|group}` 과 `target_id` (scope=group 시 그룹 id). 향후 "그룹 단위 리셋" 지원 위해. blocking 아님.

`mqtt-topics.md` PR 은 GB 가 올리면 Edge 리뷰 후 수락.

### Item 3·4·5 (Edge 관할 밖)

- **Item 3**: GB DELETE 쿼리 범위 — Edge 무관. `mode='demo'` 전체 삭제 + VEN별 옵션 수용.
- **Item 4**: VW 원본 테이블 분리 — Edge 무관. `store_energy_hourly` 별도 유지 동의.
- **Item 5**: UI — Edge 엔지니어링 그래프에 "데모 리셋" 버튼 추가 여부는 별도 결정 (Fleet Monitor 링크 근처). VW/GB UI 구현 완료 후 판단.

---

## Edge 팀 선제 조치 계획

GB 리셋 API 확정 후:

1. `src/engineering/demo_reset_listener.py` 신설 — `fleet/demo/reset/+` 구독, 콜백으로 ReplayDriver 상태 리셋
2. `ReplayDriver.reset()` 메서드 추가 — `_start_wall = _now()` + queue 폐기
3. `config.py` `REPLAY_SPEED` 기본값 60 → 360 상향 (별도 커밋)
4. `config.py` `REPLAY_MODE` env 신설 (wallclock|shift, 기본 wallclock)
5. 테스트 — DemoResetListener + reset() 단위 (FleetMonitor 패턴 참조)

---

## Edge 팀 E2E 검증 (2026-04-21 13:30)

Edge 가 `main.py` 에 `DemoResetListener` 배선 (edge-agent commit 대기) 후 docker 이미지 재빌드 → 4 컨테이너 `up -d --force-recreate` → 실 검증:

1. **리셋 API 호출**:
   ```
   POST http://127.0.0.1:8002/api/v1/gridbridge/demo/reset
   body: {"scope":"all","reason":"edge-r15-verify"}
   → {"deleted":360829,"scope":"all","target_id":null,"mqtt_published":true}
   ```

2. **MQTT 수신 확인** (edge-ven-store-001 log):
   ```
   DemoResetListener subscribed all + VEN-STORE-001
   demo/reset 수신: target=all reason=edge-r15-verify
   __main__: demo/reset 처리 시작 target=all reason=edge-r15-verify
   ReplayDriver reset: ven=VEN-STORE-001 start_sim=2026-04-21T13:29:58.921032+09:00
   ```

3. **모든 컨테이너 동시 수신**: VEN-STORE-001·002·EP-MAPO-OFFICE-01·APT-01 모두 1초 이내 처리 시작 확인.

4. **ReplayDriver.reset()** 호출 → `_start_wall` + `_start_sim = NOW()` 갱신 (옵션 B wallclock).

**Edge 측 모든 구현 실동작 확인. Items 1·3·5 RESOLVED → CLOSED 전이.**

### Edge 추가 구현

- `src/main.py` 에 DemoResetListener 생성 + `on_demo_reset` 콜백 (driver.reset + queue.purge)
- `src/telemetry/queue.py` `purge()` 메서드 (전체 outbox 폐기)
- 테스트 2건 (`test_purge_removes_all`·`test_purge_empty_returns_zero`)
